class VideoScreenArgs {
  final bool courseVideo;
  final List videos;
  final String videoTitle;
  final String contextTitle;

  VideoScreenArgs(
      this.courseVideo, this.videos, this.videoTitle, this.contextTitle);
}
